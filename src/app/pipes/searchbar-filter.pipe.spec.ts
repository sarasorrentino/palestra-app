import { SearchbarFilterPipe } from './searchbar-filter.pipe';

describe('SearchbarFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchbarFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
